<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %>
    <%= f.number_field :id %>
  </div>

  <div class="field">
    <%= f.label :id_issued %>
    <%= f.check_box :id_issued %>
  </div>

  <div class="field">
    <%= f.label :forum_name %>
    <%= f.text_field :forum_name %>
  </div>

  <div class="field">
    <%= f.label :full_name %>
    <%= f.text_field :full_name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :vehicle_desc %>
    <%= f.text_field :vehicle_desc %>
  </div>

  <div class="field">
    <%= f.label :date_joined %>
    <%= f.date_select :date_joined %>
  </div>

  <div class="field">
    <%= f.label :has_facebook %>
    <%= f.check_box :has_facebook %>
  </div>

  <div class="field">
    <%= f.label :comments %>
    <%= f.text_area :comments %>
  </div>

  <div class="field">
    <%= f.label :last_activity %>
    <%= f.date_select :last_activity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
